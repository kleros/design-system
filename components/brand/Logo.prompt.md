**Logo** — the Kleros wordmark or symbol. Renders in `currentColor`, so set `color` to theme it (white on deep-purple, `#220050` on light).

```jsx
<Logo height={32} style={{ color: '#fff' }} />
<Logo variant="symbol" height={40} style={{ color: '#9013FE' }} />
```
