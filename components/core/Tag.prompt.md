**Tag & Badge** — `Tag` is a pill chip (filters, use-case selectors); `Badge` is a compact status label.

```jsx
<Tag selectable selected>DeFi</Tag> <Tag selectable>Insurance</Tag>
<Badge variant="success" dot>Resolved</Badge>
<Badge variant="warning">Appeal period</Badge>
```
