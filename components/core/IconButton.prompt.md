**IconButton** — circular 40px button for a single icon (close, menu, theme toggle).

```jsx
<IconButton label="Menu"><Icon name="IconsMenu" /></IconButton>
<IconButton outline label="Toggle theme"><Icon name="IconsMoon" /></IconButton>
```
