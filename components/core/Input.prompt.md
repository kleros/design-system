**Input** — single-line text field with optional label, helper text and error state.

```jsx
<Input label="Email" placeholder="you@example.com" />
<Input label="Stake" error helpText="Insufficient PNK balance" />
```
